/* function COLLISIONBOXDETECT(one,two) { // Box on Box Collision;
	if (one.x < two.x + two.w &&
		one.x + one.w > two.x &&
		one.y < two.y + two.h &&
		one.y + one.h > two.y) {
			return true};
	return false;
}; */